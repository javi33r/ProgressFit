<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.progressfitprueba">

    <!-- Configuración de la aplicación -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Declarar la actividad principal -->
        <activity android:name=".MainActivity"
            android:exported="true"> <!-- "exported" es obligatorio para Android 12 o superior -->
            <intent-filter>
                <!-- Indicar que esta actividad es el punto de entrada de la aplicación -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
